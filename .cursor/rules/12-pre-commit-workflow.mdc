---
description: Mandatory pre-commit workflow - ALWAYS execute before committing
alwaysApply: true
---

# Pre-Commit Workflow (MANDATORY)

**CRITICAL**: Before creating ANY commit, you MUST execute ALL steps in order.
Do NOT skip steps. Do NOT commit if any step fails.

**One command (recommended)** — runs backend + web + CLI checks aligned with CI (auto-detects changed paths):

```bash
./scripts/pre-commit.sh
```

Per-component (matches `.github/workflows/*-ci.yml`):

```bash
./scripts/ci-backend.sh
./scripts/ci-web.sh
./scripts/ci-cli.sh
```

Requires: JDK 21, Docker (for backend `integrationTest`), Node.js 22, Go 1.22+, **golangci-lint** (one-time: `cd cli && make install-tools`).

Optional faster path when you know scope (use sparingly):

```bash
SKIP_INTEGRATION=1 ./scripts/pre-commit.sh   # skip Testcontainers integration tests
SKIP_E2E=1 ./scripts/pre-commit.sh           # skip Playwright e2e
SKIP_CLI=1 ./scripts/pre-commit.sh           # skip CLI (golangci-lint + go test)
SKIP_BACKEND=1 ./scripts/pre-commit.sh         # skip backend only
SKIP_WEB=1 ./scripts/pre-commit.sh             # skip web only
FORCE_ALL=1 ./scripts/pre-commit.sh          # run all components (ignore path detection)
```

---

## Step 1: Format Code (backend)

```bash
cd backend && ./gradlew spotlessApply --no-daemon
cd backend && ./gradlew spotlessCheck --no-daemon
```

**Verify**: Command exits with `BUILD SUCCESSFUL` (CI runs `spotlessCheck` in `backend-ci.yml`)

## Step 1b: Static analysis (backend)

```bash
cd backend && ./gradlew check -x test --no-daemon
```

**Verify**: Matches `Backend · Format & lint` job in `backend-ci.yml`

## Step 2: Compile and Check for Warnings (backend)

```bash
cd backend && ./gradlew compileJava compileTestJava --no-daemon
```

**Verify**: No compilation errors, review any warnings

## Step 3: Run Backend Unit Tests

```bash
cd backend && ./gradlew test --no-daemon
```

**Verify**: All tests pass. If tests fail, FIX THEM before proceeding.

## Step 4: Run Backend Integration Tests

```bash
cd backend && ./gradlew integrationTest --no-daemon
```

**Verify**: All integration tests pass. Requires Docker (Testcontainers).

Matches `backend-ci.yml` integration job. Skip only for docs-only commits (see below).

## Step 5: Web — Lint, Unit Tests, E2E, Build

Same as `.github/workflows/frontend-ci.yml`:

```bash
cd web && npm ci
cd web && npm run lint          # TypeScript (tsc -b)
cd web && npm run test          # Vitest + Testing Library
cd web && npx playwright install chromium
cd web && npm run test:e2e      # Playwright E2E
cd web && npm run build         # tsc + vite build
```

**Verify**: Each command exits 0.

First-time Playwright: `npx playwright install chromium` (or `--with-deps chromium` on Linux CI).

## Step 5b: CLI — Lint, test, build

Same as `.github/workflows/cli-ci.yml` (or `./scripts/ci-cli.sh`):

```bash
cd cli && make install-tools            # once: pinned golangci-lint (cli/.golangci-version)
export PATH="$(go env GOPATH)/bin:$PATH"
./scripts/ci-cli.sh                     # or: cd cli && make lint && make test && make build
```

**Verify**: `golangci-lint version` prints the version in `cli/.golangci-version`. Config: `cli/.golangci.yml`.

## Step 6: Check for Lint Issues (IDE)

```bash
# Use ReadLints tool on modified files
```

**Verify**: No new linter errors introduced

## Step 7: Review Changes

Before staging, review what you're committing:

```bash
git diff --stat
git diff  # Review actual changes
```

**Verify**:
- [ ] No debug code (System.out.println, console.log, etc.)
- [ ] No commented-out code blocks
- [ ] No hardcoded secrets or credentials
- [ ] No unintended file changes
- [ ] All new files are intentional

## Step 8: Self Code Review

Ask yourself:
- [ ] Does this code match the design in `docs/lld/`?
- [ ] Would I understand this code in 6 months?
- [ ] Are error cases handled properly?
- [ ] Is there test coverage for new code?
- [ ] Are there any TODOs without issue links?

## Step 9: Stage and Commit

Only after ALL above steps pass:

```bash
git add -A
git commit -m "$(cat <<'EOF'
type(scope): Clear description of what changed

- Bullet point explaining the change
- Another bullet if needed

Refs: #issue or US-XX.YY (if applicable)
EOF
)"
```

## Step 10: Post-Commit Verification

```bash
git log -1  # Verify commit message looks correct
git status  # Verify working directory is clean
```

---

## Quick Reference Commands

```bash
# Full pre-commit (auto-detects backend/, web/, cli/ changes)
./scripts/pre-commit.sh

# Force full suite
FORCE_ALL=1 ./scripts/pre-commit.sh

# Per component
./scripts/ci-backend.sh
./scripts/ci-web.sh
./scripts/ci-cli.sh
```

## When to Skip (NEVER for production code)

The ONLY acceptable reasons to skip steps:

| Change type | Skip |
|-------------|------|
| Documentation only (`docs/**`, `*.md`) | Auto-skipped by `pre-commit.sh`; still do 7–10 |
| Backend only | `SKIP_WEB=1 SKIP_CLI=1` or rely on path detection |
| Web only | `SKIP_BACKEND=1 SKIP_CLI=1` |
| CLI only | `SKIP_BACKEND=1 SKIP_WEB=1` |
| Config / formatting only | Integration/unit if no logic changed; still format |

Do **not** skip component tests on cross-cutting or security changes. Use `FORCE_ALL=1` before release merges.

## Common Issues and Fixes

### Spotless Fails
```bash
cd backend && ./gradlew spotlessApply --no-daemon  # Auto-fix
```

### Backend Tests Fail
- Read the failure message
- Fix the code or test
- Re-run tests
- Do NOT bypass with `@Disabled`

### Integration Tests Fail
- Ensure Docker is running
- Re-run: `cd backend && ./gradlew integrationTest --no-daemon`

### Web: `npm ci` Fails
- Use Node.js 22 (same as CI)
- Delete `web/node_modules` and re-run `npm ci`

### Playwright E2E Fails Locally
- Install browser: `cd web && npx playwright install chromium`
- If port 5173 is in use, stop other dev servers or let Playwright reuse (see `web/README.md`)

### Compilation Errors
- Fix all errors before proceeding
- Do NOT commit code that doesn't compile

---

**REMEMBER**: A broken commit wastes everyone's time. Take a few minutes to verify
rather than hours debugging a broken build.
