Pravah platform ({{ .Chart.AppVersion }}) installed in namespace {{ .Release.Namespace }}.

API gateway (cluster):
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "pravah.fullname" . }}-gateway {{ .Values.services.gateway.port }}:{{ .Values.services.gateway.port }}

Then open the UI (separate process): cd web && npm run dev — API base http://localhost:{{ .Values.services.gateway.port }}

Seed demo data (after all pods are Ready):
  ./scripts/deploy/k8s-local-seed.sh {{ .Release.Namespace }}

Sign in: dev@localhost.pravah / PravahDev1!  (after seed)

Alpha K8s note: container stages are disabled (PRAVAH_CONTAINER_ENABLED=false). Use echo/sql stages.

Docs: deploy/README.md
