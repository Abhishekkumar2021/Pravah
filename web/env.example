# Optional: absolute gateway URL when not using Vite dev proxy
# VITE_PRAVAH_API_BASE=https://api.example.com

# Optional: gateway for Vite proxy (npm run dev)
# VITE_DEV_PROXY_TARGET=http://localhost:8080
