# Prefer env.local.example — copy to .env.local (gitignored):
#   cp env.local.example .env.local
# Or from repo root:  make local-setup

# See env.local.example for all Vite variables and defaults aligned with backend/.env
