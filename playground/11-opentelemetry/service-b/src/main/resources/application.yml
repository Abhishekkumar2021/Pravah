server:
  port: 8081

spring:
  application:
    name: service-b

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    io.pravah.playground.otel: DEBUG
    io.opentelemetry: INFO
